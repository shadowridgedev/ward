package com.myexperiments.ward;

public class JPADemo {
	public static void main(String[] args) {
		/*
		 * User user = new User(); user.setId(1); user.setAge(20); user.setName("Abc");
		 * try (JpaRepository userJpaRepository = new JpaRepository<BlenderMaster,
		 * Long>(BlenderMaster.class)) { userJpaRepository.save(user); } catch
		 * (Exception e) { e.printStackTrace(); }
		 */
	}

}